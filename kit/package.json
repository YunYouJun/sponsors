{
  "private": true,
  "scripts": {
    "build": "sponsorkit -f",
    "generate": "sponsorkit -w 800 -f",
    "generate:wide": "sponsorkit -f -w 1300 --name sponsors.wide --dir ../public"
  },
  "devDependencies": {
    "sponsorkit": "latest",
    "@sponsors/utils": "workspace:*",
    "@sponsors/types": "workspace:*",
    "afdian-api": "^1.0.4"
  }
}
